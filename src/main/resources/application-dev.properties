# ============================================================================
# APPLICATION-DEV.PROPERTIES - LOCAL DEVELOPMENT PROFILE
# Use: SPRING_PROFILES_ACTIVE=dev or don't set anything (default)
# ============================================================================

# Database Configuration - Local PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5434/logistics_db
spring.datasource.username=scb_user
spring.datasource.password=scb@1475
spring.datasource.driver-class-name=org.postgresql.Driver

# Database Connection Pool
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# JPA / Hibernate - Development Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Database Initializer Flags
app.db.init.enabled=true
app.db.init.drop=false
app.db.init.seed=true

# OpenRouteService API Key (Local/Development)
openrouteservice.api.key=eyJvcmciOiI1YjNjZTM1OTc4NTExMTAwMDFjZjYyNDgiLCJpZCI6ImE4ZmFlZjFiNmFmMjQyNjQ4ZDA5MzBiOGRjMzIzMDlhIiwiaCI6Im11cm11cjY0In0=

# JWT Configuration (Development)
security.jwt.secret=Y2hpZWZzdHJhaWdodHN1Y2Nlc3NmdWxiYXNpY2FueW9uZXN0YXl0cmlwY2xpbWJ1c2U=
jwt.expiration=86400000  # 24 hours in milliseconds

# CORS Configuration for Local Development
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:8080,http://localhost:8081
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
app.cors.allowed-headers=Authorization,Content-Type,X-Requested-With,Accept,Origin,Access-Control-Request-Method,Access-Control-Request-Headers
app.cors.allow-credentials=true
app.cors.max-age=3600

# Logging - Verbose for Development
logging.level.root=INFO
logging.level.com.pgsa.trailers=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.hibernate.stat=DEBUG
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE

# Spring Security Debug
#logging.level.org.springframework.security.web.FilterChainProxy=DEBUG

# Server Port (Development - can be overridden by PORT env var)
server.port=${PORT:8081}

# HTTP/2 Support
server.http2.enabled=false

# Jackson Serialization
spring.jackson.serialization.indent-output=true
spring.jackson.date-format=com.fasterxml.jackson.databind.util.StdDateFormat
spring.jackson.time-zone=UTC

# ============================================================================
# DEVELOPMENT-SPECIFIC OVERRIDES
# ============================================================================
spring.mvc.log-request-details=true
spring.mvc.log-resolved-exceptions=true
